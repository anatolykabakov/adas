<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Adas"
        tools:targetApi="31"
        android:extractNativeLibs="true">

        <!-- OpenCL для раннера thneed (исполнение модели на GPU).
             Начиная с targetSdk 31 приложение обязано объявить вендорную библиотеку явно, иначе её не видно
             из namespace классзагрузчика: на OnePlus 7T dlopen давал "library not found" по короткому имени и
             "is not accessible for the namespace clns-7" по абсолютному пути, хотя libOpenCL.so перечислен в
             /vendor/etc/public.libraries.txt. targetSdk у нас 34.
             required="false" намеренно: устройства без OpenCL должны ставить приложение и работать на ONNX. -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />

        <activity
            android:name="ai.flow.adas.MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
             <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
        <activity
            android:name="ai.flow.adas.TrafficYoloBenchActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:label="Traffic YOLO Bench" />
        <service android:name="ai.flow.adas.AdasAppHandler"></service>
        <service android:name="ai.flow.adas.ZMQBridgeService"></service>
    </application>

</manifest>
