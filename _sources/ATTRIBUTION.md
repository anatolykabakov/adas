# Attribution

Figures under `Control/figures/` (PNG) and `Vision/figures/` are derived from
[Algorithms for Automated Driving](https://github.com/thomasfermi/Algorithms-for-Automated-Driving)
(CC BY 4.0) or generated for this course.

Readers in VS Code / Cursor: chapters use normal Markdown images so the preview shows them.
MyST `{figure}` blocks are avoided on purpose (they do not render in the IDE).
Jupyter Book still builds these Markdown images fine.
